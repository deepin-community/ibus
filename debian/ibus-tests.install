# Do not install ibus-desktop-testing-runner to /usr/bin. It is a test-only
# command. It doesn't need a man page. And it's only useful in a contained
# test environment because it touches files under /var/lib and $HOME for
# tests. Regular users should not run it.
usr/bin/ibus-desktop-testing-runner usr/libexec/
usr/libexec/installed-tests/*
usr/share/installed-tests/*
